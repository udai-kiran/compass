# Secrets — never commit
.env
.env.local

# Dependencies & builds
node_modules/
dist/
build/
.docusaurus/

# Local data (attachments, backups)
data/

# Logs & OS noise
*.log
.DS_Store

.claude/

.idea/
*.pdf
Pasted image*

# Literal secret-value regexes (dev credential pair, internal IPs) used with
# `grep -f .secret-patterns` so evidence/report files never need to quote them.
.secret-patterns
