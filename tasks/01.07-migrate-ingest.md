---
id: "1.7"
title: Migrate ingest module
phase: "1 — Module migration"
release: "2.0.0"
status: todo
depends: [1.1]
---

Routes: imports, inbox, mailboxes. Tables: imports, import_rows, import_presets, mailbox_accounts, mailbox_credentials, email_ingestions, extracted_transactions.

`inbox.ts` is 804 lines doing several jobs at once — review-queue CRUD, the extraction review state machine, and transfer/repayment classification — and should be split accordingly. `import_rows` has no `user_id` and scopes via its parent import.

The out-of-process `apps/ingestor` and `apps/extractor` read and write several of these tables directly. Their SQL must keep working unchanged — the migration must not alter table names or columns.

## Acceptance criteria
- [ ] Route snapshot unchanged; no migration diff; `backup.test.ts` green
- [ ] `inbox.ts` split into review-queue, state-machine, and classification units
- [ ] `apps/ingestor` and `apps/extractor` still typecheck and their tests still pass
- [ ] The `email.extract` / `ingestor.run` queue contracts are untouched
- [ ] Accepting an inbox draft still emits `ledger.mutated` (from 0.2)
- [ ] typecheck + lint + test green
