No findings.

All seven checks pass:

- Correct reset endpoint and mutation pattern; no unused imports.
- `MailboxRow` props are correctly extended.
- Sync runs only after reset succeeds.
- All three toast paths exist.
- Button state, label, type, and confirmation are correct.
- `SYNC_WINDOW_MINUTES[0]` correctly resolves to 5 minutes.
- Existing remove/sync behavior is unchanged.
- Web typecheck passes.