# Implementation 1 — Migration Collapse

## Files Inspected
- `tasks/044-migration-collapse/TASK.md`
- `tasks/044-migration-collapse/investigation-1.md`

## Files Deleted
- `apps/api/drizzle/*.sql` — 69 files deleted
- `apps/api/drizzle/meta/` — directory with 70 JSON files deleted

## Files Created
- `apps/api/drizzle/0000_nosy_lizard.sql` — generated by drizzle-kit
- `apps/api/drizzle/meta/0000_snapshot.json` — generated by drizzle-kit
- `apps/api/drizzle/meta/_journal.json` — generated by drizzle-kit (1 entry)

## Commands Run

### Step 1 — Pre-deletion count
```
ls apps/api/drizzle/*.sql | wc -l       → 69
ls apps/api/drizzle/meta/*.json | wc -l → 70
```

### Step 2 — Delete migrations
```bash
rm apps/api/drizzle/*.sql
rm -rf apps/api/drizzle/meta/
```
Output: "SQL files deleted", "meta/ deleted"

### Step 3 — Generate baseline
```bash
DATABASE_URL=postgres://x:x@localhost/x npm run db:generate -w apps/api
```
Output (abridged):
```
50 tables
...
[✓] Your SQL migration file ➜ drizzle/0000_nosy_lizard.sql 🚀
```

### Step 4 — Append tsvector column + GIN index
Appended at end of `0000_nosy_lizard.sql` (after last existing `--> statement-breakpoint`):
```sql
--> statement-breakpoint
ALTER TABLE "transactions" ADD COLUMN "search" tsvector GENERATED ALWAYS AS (to_tsvector('simple', coalesce("merchant", '') || ' ' || coalesce("notes", ''))) STORED;
--> statement-breakpoint
CREATE INDEX "transactions_search_idx" ON "transactions" USING gin ("search");
```
Note: `check_split_sum` function and triggers were NOT appended — `transaction_splits` was dropped in task 043.

## Verification Results

| Check | Command | Result |
|---|---|---|
| tsvector present | `grep -c 'tsvector' 0000_nosy_lizard.sql` | **1** ✓ |
| transactions_search_idx present | `grep -c 'transactions_search_idx' 0000_nosy_lizard.sql` | **1** ✓ |
| Dropped items absent | `grep -c 'transaction_splits\|transfer_links\|check_split_sum\|is_opening' 0000_nosy_lizard.sql` | **0** ✓ |
| SQL file count | `ls apps/api/drizzle/*.sql \| wc -l` | **1** ✓ |
| Meta JSON count | `ls apps/api/drizzle/meta/*.json \| wc -l` | **2** ✓ |

## transactions CREATE TABLE Verification
Lines 682–699 of `0000_nosy_lizard.sql` — `transactions` CREATE TABLE contains:
- `id`, `user_id`, `date`, `occurred_at`, `merchant`, `notes`, `tags`, `source`, `policy_id`, `resource_id`, `sip_id`, `recurring_template_id`, `reconciled_statement_id`, `deleted_at`, `created_at`, `updated_at`
- Does NOT contain `amount_paise`, `category_id`, or `account_id` (those live on `postings`) ✓
- `CREATE TABLE "postings"` is present ✓

## Assumptions
- `check_split_sum` trigger function + constraint triggers were intentionally omitted per the brief: "task 043 (legacy ledger drop) — transaction_splits dropped".
- The investigation-1.md recommendation to append those triggers was superseded by the brief's explicit exclusion.

## Unresolved Risks
- AC4/AC5/AC6 (db:migrate on fresh DB, db:seed, npm run test) were not run — no live Postgres available in the sandbox. These steps remain in the task plan (P4–P7) and should be verified on a machine with a running Postgres instance.
