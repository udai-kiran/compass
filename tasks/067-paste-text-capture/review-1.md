**Findings**

BLOCKING: Adding `"shopping_parse"` to `AiEventKindSchema` will break the web build unless the exhaustive event-label map is updated. [EventLogPage.tsx](/work/personal/compass/apps/web/src/routes/events/EventLogPage.tsx:5) declares `KIND_LABELS: Record<AiEventKind, string>` and currently has entries only through `goal_roadmap` at line 12. The plan’s edit scope does not include this file, but after [ai-events.ts](/work/personal/compass/packages/shared/src/schemas/ai-events.ts:4) adds a new enum member, TypeScript should require a `shopping_parse` label. The filter list at [EventLogPage.tsx](/work/personal/compass/apps/web/src/routes/events/EventLogPage.tsx:15) is not type-exhaustive, so updating filters is product choice, but the label map is mandatory.

non-blocking: The migration claim is correct: `aiEvents.kind` is a real Drizzle `pgEnum`, not a text column. The enum is defined as `pgEnum("ai_event_kind", ...)` at [automation/schema.ts](/work/personal/compass/apps/api/src/modules/automation/schema.ts:60), and the table column is `kind: aiEventKind("kind").notNull()` at [automation/schema.ts](/work/personal/compass/apps/api/src/modules/automation/schema.ts:85). Adding the value therefore requires a DB migration, expected as `ALTER TYPE "public"."ai_event_kind" ADD VALUE 'shopping_parse'`.

non-blocking: The `ALTER TYPE ADD VALUE` transaction concern is real but acceptable for the current project pattern as long as the migration only adds the value and does not use it in the same migration. The production bootstrap uses `drizzle-orm/node-postgres/migrator` at [bootstrap.ts](/work/personal/compass/apps/api/src/db/bootstrap.ts:12), called at [bootstrap.ts](/work/personal/compass/apps/api/src/db/bootstrap.ts:53). Drizzle’s PG dialect wraps pending migrations in `session.transaction(...)` at `node_modules/drizzle-orm/pg-core/dialect.js:60`, then executes each statement at line 64. There is already an enum-only migration doing exactly this for `goal_roadmap` at [0004_parched_colonel_america.sql](/work/personal/compass/apps/api/drizzle/0004_parched_colonel_america.sql:1). This is safe on modern/Postgres 18 setups if the value is not referenced until after commit; older PostgreSQL versions before transactional enum additions would be a deployment compatibility risk, but the repo has already accepted that risk with migration 0004.

non-blocking: I found no existing backup or AI-event test that hard-codes the AI event kind count. Backup coverage is table-level: `ai_events` is already in `ALL_TABLES` at [backup.ts](/work/personal/compass/apps/api/src/modules/system/services/backup.ts:46) and `USER_TABLES` at [backup.ts](/work/personal/compass/apps/api/src/modules/system/services/backup.ts:70). The schema decomposition test counts exported Drizzle enum objects, not enum values, at [schema.decomposition.test.ts](/work/personal/compass/apps/api/src/db/schema.decomposition.test.ts:148), so adding a value does not affect that count. Route snapshots will need regeneration because the new route changes the route surface; that is already in the plan at [TASK.md](/work/personal/compass/tasks/067-paste-text-capture/TASK.md:90).

The three-way tool-call discipline is faithfully described. The extractor gates structured mode on `ai.name !== "ollama"` at [extract.ts](/work/personal/compass/apps/extractor/src/extract.ts:394), sends either `[RECORD_TXNS_TOOL]` plus `toolChoice` or no tools at [extract.ts](/work/personal/compass/apps/extractor/src/extract.ts:395), filters exact tool names at [extract.ts](/work/personal/compass/apps/extractor/src/extract.ts:410), then handles `1`, `0`, and `2+` matches at [extract.ts](/work/personal/compass/apps/extractor/src/extract.ts:412). Extracting that into a pure `parseItemsFromTurn(turn, structured)` is sound and testable; the helper should mirror the logic exactly, including not consulting `turn.text` on `2+` matching calls.

The AI-disabled degrade is correct. `getUserAiProvider` returns `NullProvider` when no row or provider `"none"` exists at [ai-settings.ts](/work/personal/compass/apps/api/src/modules/automation/services/ai-settings.ts:85), and also returns `NullProvider` on disallowed base URL at line 90. `NullProvider.enabled` is `false` at [null-provider.ts](/work/personal/compass/packages/ai/src/null-provider.ts:9), and its `chat()` would throw if called at line 18, so the planned early `!ai.enabled` return is the right shape.

AC6 review-only is the right call. The board says parsed items must land in review, not saved, at [09.04-paste-text-capture.md](/work/personal/compass/tasks/09.04-paste-text-capture.md:22). The only current list write path is `addItem`, which inserts into `shopping_list_items` at [lists.ts](/work/personal/compass/apps/api/src/modules/shopping/services/lists.ts:181). The plan explicitly says the parse route must not call `addItem` or insert into `shopping_list_items` at [TASK.md](/work/personal/compass/tasks/067-paste-text-capture/TASK.md:67), and includes a hermetic no-write/no-`addItem` test at [TASK.md](/work/personal/compass/tasks/067-paste-text-capture/TASK.md:94). No staging table is needed for this acceptance criterion.

Quantity normalization lines up. `convertToBaseQuantity` exists with signature `quantity: string, displayUnit: "kg" | "g" | "litre" | "ml" | "piece"` at [money.ts](/work/personal/compass/packages/shared/src/money.ts:114), returning `{ quantityBase, unit }` at line 117. `DisplayUnitSchema` accepts the same units at [shopping.ts](/work/personal/compass/packages/shared/src/schemas/shopping.ts:253). Catching conversion errors and leaving both `quantityBase` and `unit` null matches the DB pairing check on `shopping_list_items` at [schema.ts](/work/personal/compass/apps/api/src/modules/shopping/schema.ts:168).

Demo/auth is correct. The shopping plugin is registered under `/api/shopping`, and route files use relative paths per [plugin.ts](/work/personal/compass/apps/api/src/modules/shopping/plugin.ts:11). The auth hook rejects unauthenticated non-public routes at [auth.ts](/work/personal/compass/apps/api/src/plugins/auth.ts:58), and rejects demo `POST` requests before route handlers unless explicitly allowlisted at [auth.ts](/work/personal/compass/apps/api/src/plugins/auth.ts:66). `/api/shopping/parse-text` would not be in the allowlist, so no handler-side `recordAiEvent` should occur for demo users.

No real prompt-injection blocker: this is user-supplied text sent to an LLM, but output is constrained by tool/schema validation, fail-closed parsing, quantity normalization, and review-only response. The larger practical risks are the missing web exhaustive map update and ensuring provider/network failures are handled deliberately. The plan’s “never a 500” language is valid for bad model output; for provider outage, follow the existing convention of soft degradation or mapped `503`, but do not accidentally catch all errors and hide programming defects.

Confirmed: the migration claim is correct, and the “no other DB schema change” claim is correct. The plan should add the web event-label update to scope and keep the enum migration isolated to `ALTER TYPE ADD VALUE` only.