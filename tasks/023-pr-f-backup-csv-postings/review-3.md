## Verdict

**CHANGES REQUIRED.**

### BLOCKER — D9.6 test assumes UUIDs follow insertion order

[backup.test.ts:1713](/home/udai/common/compass/apps/api/src/modules/system/services/backup.test.ts:1713)–[1727](/home/udai/common/compass/apps/api/src/modules/system/services/backup.test.ts:1727) assumes the first inserted posting has the lowest ID.

That is false: `postings.id` is a random UUID generated by `defaultRandom()` at [ledger.ts:135](/home/udai/common/compass/apps/api/src/db/shared/ledger.ts:135). PostgreSQL’s `order by p.id` is deterministic for fixed UUID values, but unrelated to insertion order. Therefore the expected Bank/`-7000` posting wins only by chance; this test will fail nondeterministically in CI.

The test must supply two explicit UUIDs with known lexical/database ordering, then assert the posting carrying the smaller UUID wins.

## Prior blockers

1. **Production unchanged — confirmed.** [backup.ts](/home/udai/common/compass/apps/api/src/modules/system/services/backup.ts) predates `review-2` and was not modified during iteration 2. Its current SQL and mapping remain exactly those approved. Current SHA-256: `1e675ee2790f571c0796503d9746087e78b279014aea4d6deb90f041444d7151`.

2. **AC2 — resolved.** Raw equality is asserted at [backup.test.ts:1346](/home/udai/common/compass/apps/api/src/modules/system/services/backup.test.ts:1346)–[1352](/home/udai/common/compass/apps/api/src/modules/system/services/backup.test.ts:1352). `toCsv` joins and terminates rows with `"\r\n"` at [csv.ts:147](/home/udai/common/compass/apps/api/src/lib/csv.ts:147), so the asserted literal is exact. `createUser()` only inserts a user; it creates no transactions, making the fixture genuinely empty.

3. **AC17 rename — resolved.** The posting is inserted using the old account name at [backup.test.ts:1688](/home/udai/common/compass/apps/api/src/modules/system/services/backup.test.ts:1688)–[1695](/home/udai/common/compass/apps/api/src/modules/system/services/backup.test.ts:1695), the account is renamed afterward at line 1697, and the new name is asserted at line 1700. Non-vacuous.

4. **D9.6 — unresolved.** Two real postings and one-row cardinality are exercised, but the winning-posting assertion is invalid because IDs are random UUIDs.

5. **AC14 comment — corrected accurately.** It now says the shared fixture supplies Food/Transport and the counter postings are referenced in reverse alphabetical order at [backup.test.ts:1557](/home/udai/common/compass/apps/api/src/modules/system/services/backup.test.ts:1557)–[1569](/home/udai/common/compass/apps/api/src/modules/system/services/backup.test.ts:1569).

## Regression sweep

Iteration 2 changed only `backup.test.ts`; `backup.ts` was untouched. The overall test-file diff contains additions only—no existing test was weakened or deleted. Unrelated extractor working-tree changes predate/belong outside this follow-up and were not part of iteration 2.

## Coverage verdict

- **Non-vacuous test code:** AC2–AC9, AC11–AC17; D9.1–D9.5.
- **Inspection-only:** AC1 and AC18.
- **Not proven:** D9.6, because its UUID-order expectation is invalid.
- **Pending CI execution:** AC2–AC17 and AC10 overall. No DB-backed tests ran locally because `DATABASE_URL`/Postgres is unavailable.
- **AC10:** remains unverified until CI runs typecheck, lint, and the DB-backed suite.
- **AC1–AC18 overall:** all implementation criteria appear satisfied, but acceptance remains blocked by the defective D9.6 test and pending CI.