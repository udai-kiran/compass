---
id: "2.3"
title: Duplicate detection
phase: "2 — Import & rules"
status: done
depends: [2.1]
---

Hash (date, amount, normalized merchant, account) for each staged row; flag matches against existing transactions and within the batch.

## Acceptance criteria
- [x] Re-importing the same file flags 100% duplicates
- [x] Overlapping statement periods flag only the overlap
- [x] User can override a false-positive flag per row
