---
id: "1.3"
title: Migrate investments module
phase: "1 — Module migration"
release: "2.0.0"
status: todo
depends: [1.1]
---

Routes: holdings (13 endpoints), sips, networth. Tables: holdings, holding_valuations, holding_events, gold_details, nps_details, account_nps_details, sips, net_worth_snapshots.

`sips.ts` is the largest service in the repo (1319 lines) and `networth.ts` (581) reaches across every domain to compute buckets — that cross-domain reach becomes the `NetWorthContributor` port in 1.9 rather than raw joins. Note `holding_valuations` and `holding_events` have no `user_id` and scope via parent FK.

## Acceptance criteria
- [ ] Route snapshot unchanged; no migration diff; `backup.test.ts` green
- [ ] `sips.ts` decomposed along date-math / lifecycle / installment-matching seams
- [ ] Net-worth account-type→bucket classification stays exhaustive (compile error on a new account type)
- [ ] Net-worth daily schedulers still registered and still on `LEDGER_DAY_TZ`
- [ ] typecheck + lint + test green
