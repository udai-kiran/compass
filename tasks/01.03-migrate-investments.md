---
id: "1.3"
title: Migrate investments module
phase: "1 — Module migration"
release: "2.0.0"
status: done
depends: [1.1]
---

Routes: holdings (16 endpoints), sips, networth, account-nps (`GET`/`PUT /api/accounts/:accountId/nps-details`). Tables: holdings, holding_valuations, holding_events, gold_details, nps_details, account_nps_details, sips, net_worth_snapshots.

`sips.ts` is the largest service in the repo (1319 lines) and `networth.ts` (581) reaches across every domain to compute buckets — that cross-domain reach becomes the `NetWorthContributor` port in 1.9 rather than raw joins. Also includes `holding-details.ts`, `capital-gains.ts`, `tax-lots.ts`, `mf-import.ts`, `xirr.ts`, `amfi.ts`, `mf-scheme-map.ts`, and `goal-networth.ts`. Note `holding_valuations` and `holding_events` have no `user_id` and scope via parent FK.

## Acceptance criteria
- [x] Route snapshot unchanged; no migration diff; `backup.test.ts` green
- [x] `sips.ts` decomposed along date-math / lifecycle / installment-matching seams (plus a 4th,
      roadmap-unnamed "committed monthly / goal-plan gap" seam — see `tasks/010-migrate-investments/`)
- [x] Net-worth account-type→bucket classification stays exhaustive (compile error on a new account type)
- [x] Net-worth daily schedulers still registered and still on `LEDGER_DAY_TZ`
- [x] typecheck + lint + test green (`apps/api` 837/837, all non-`apps/extractor` workspaces green;
      `apps/extractor` has one pre-existing, unrelated `DATABASE_URL`-env gap in its own `package.json`
      test script, untouched by this task — see `tasks/010-migrate-investments/TASK.md`'s final status
      for the full waiver rationale)

Full implementation record: `tasks/010-migrate-investments/` (`TASK.md`, `DELEGATION.md`,
`investigation-1.md`, `investigation-2.md`, `implementation-1.md`, `verification-1.md`,
`review-1.md`/`review-2.md`/`review-3.md`).
