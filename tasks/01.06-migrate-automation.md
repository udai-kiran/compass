---
id: "1.6"
title: Migrate automation/AI module
phase: "1 — Module migration"
release: "2.0.0"
status: done
depends: [1.1]
---

Routes: ai, ai-events. Tables: ai_settings, ai_events. Services: `services/ai/*` (assistant, categorize, events, summary, tools — 518 lines total).

`services/ai/` is already the only sub-grouped folder in the codebase, so this is the closest thing to a pre-existing module and should migrate cleanly. Two invariants to protect: AI is resolved **per-user** from `ai_settings` with no global provider, and the `AiObserver` → `ai_events` write stays fire-and-forget so it can never gate or slow a model call.

**Done in `tasks/016-migrate-automation/` — moved into `modules/automation/`.** Also moved `services/ai-settings.ts` + its test (it owns `ai_settings` and is the per-user provider resolution), repointing the one external consumer `routes/auth.ts`. Tables stay thin-re-exported from `db/schema.ts` (physical relocation deferred to 1.9, like every prior module). The two AI registrations were already adjacent and in order, so wrapping them in `automationRoutes` left **both** route snapshots byte-identical (this corrected an initial plan assumption that the raw `printRoutes()` table would change). Independently verified (`verification-1.md`) and Codex-reviewed (`review-2.md`, no runtime findings).

## Acceptance criteria
- [x] Route snapshot unchanged; no migration diff; `backup.test.ts` green — both `route-surface`/`route-table` snapshots byte-identical (sha unchanged); `db:generate` → "No schema changes"; `backup.test.ts` 13/13
- [x] Per-user provider resolution unchanged; still no global AI provider — `getAiSettings`/`getUserAiProvider`/`upsertAiSettings` bodies byte-identical; no global provider introduced
- [x] AI event logging still fire-and-forget; a failing observer never breaks a model call — the `observe` arrow is unchanged (returns `recordAiEvent(...)`; not `void`-wrapped); fire-and-forget stays in the untouched `packages/ai/src/http.ts` `void report(...)`
- [x] The assistant tool loop (`get_spending_summary`, `get_budget_status`, `get_financial_health`, `search_transactions`, `list_goals`) still works after tools cross module boundaries — `tools.ts` bodies unchanged; cross-module imports repointed to sibling modules `../../planning|ledger/services`
- [x] App still boots and the full suite still passes with AI disabled — `apps/api` 848 → 853 (+5); root suite exit 0
- [x] typecheck + lint + test green — typecheck exit 0, lint exit 0, `apps/api` 853/853
