---
id: "4.5"
title: Household & split API routes
phase: "4 — Household & Split"
release: "2.1.0"
status: done
depends: [4.4]
---

Routes for household management (create, invite, accept, leave, list members), per-record sharing grants, split create/edit/delete, balance summary, and settle-up.

Because these are new routes rather than moved ones, the Phase 0 route snapshot is intentionally updated here — the first sanctioned change to it since the migration began.

## Acceptance criteria
- [ ] Full CRUD for households, membership, grants, splits and settlements
- [ ] All requests/responses typed by new Zod schemas in `packages/shared`
- [ ] Route snapshot updated deliberately, with the new routes reviewed
- [ ] Every mutating route rejected for a demo session
- [ ] typecheck + lint + test green
