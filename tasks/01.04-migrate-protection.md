---
id: "1.4"
title: Migrate protection module
phase: "1 — Module migration"
release: "2.0.0"
status: done
depends: [1.1]
---

Routes: insurance, retirement. Tables: insurance_policies, insurance_health_cards, retirement_details.

The smallest domain — a good early confidence check on the migration recipe. Includes policy-document and health-card uploads, so it exercises the `Storage` abstraction across a module boundary.

## Acceptance criteria
- [x] Route snapshot unchanged; no migration diff; `backup.test.ts` green (`route-surface.snapshot.txt`
      and `route-table.snapshot.txt` both byte-identical to HEAD, sha256-confirmed; `npm run db:generate`
      produced a zero diff confirmed by a 139-file content-hash manifest; `backup.test.ts` passed 13/13
      with `backup.ts` unmodified)
- [x] The `Storage` seam is unchanged by the move; live disk-vs-S3 verification is task 1.10 (the 7
      `app.storage` call sites and 7 `Storage`-taking service signatures are intact and typecheck is
      clean; live disk-vs-S3 verification remains gated behind task 1.10, which task 1.9 now depends on)
- [x] typecheck + lint + test green (typecheck exit 0 and lint exit 0 across all 7 workspaces; `apps/api`
      842/842, up from the 837 baseline — +5 new tests: 2 schema-smoke, 1 plugin, 2 demo-403; all other
      workspaces green: web 264/264, shared 212/212, ai 32/32, ingestor 12/12. The root `npm run test`
      exits 1 solely because of the pre-existing, unrelated `apps/extractor` `DATABASE_URL` packaging
      gap — its test script lacks the `--env-file-if-exists=../../.env` flag every other workspace has —
      the identical waiver documented by task 1.3)

Full implementation record: `tasks/011-migrate-protection/` (`TASK.md`, `DELEGATION.md`,
`investigation-1.md`, `implementation-1.md`, `backend-1.md`, `verification-1.md`,
`review-1.md`/`review-2.md`/`review-3.md`/`review-4.md`).
