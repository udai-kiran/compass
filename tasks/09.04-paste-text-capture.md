---
id: "9.4"
title: Paste-text list capture
phase: "9 — Shopping core"
release: "2.3.0"
status: done
depends: [9.3]
---

Turn free text ("2kg atta, milk 1L, 6 eggs, dal") into structured `(item, qty, unit)` rows using the existing AI path — `createAiProvider` → `chat({ tools: [SPEC], toolChoice: SPEC.name })` → filter tool calls by exact name → Zod `safeParse`.

Reuse the extractor's discipline verbatim: gate structured output on `ai.name !== "ollama"` with a prose `extractJson` fallback, **fail closed on 2+ matching tool calls**, and never crash on unparseable output — return nothing and keep the raw input for replay. Recipe → ingredient list is the same call with a different prompt.

Adds a new `ai_events` kind (the enum in `packages/shared/src/schemas/ai-events.ts` is closed).

## Acceptance criteria
- [x] Free text and recipe text both produce structured, reviewable items
- [x] Unparseable model output yields an empty result and a clear message — never a 500
- [x] Two matching tool calls fail closed without falling back to prose
- [x] Works with a non-tool-calling provider via the prose fallback
- [x] New `ai_events` kind added and recorded per call
- [x] Parsed items land in review, not directly on a saved list
- [x] typecheck + lint + test green

**Done.** Committed `1095b71` on `feat/shopping-core-capture`. `POST /api/shopping/parse-text` reuses
the extractor's three-way tool-call discipline (fail closed on ≥2; prose `extractJson` fallback for
non-tool providers), adds the `shopping_parse` ai_event kind (enum + `ALTER TYPE` migration), recorded
per call; review-only (no list write). Codex code review clean after iteration 2 (blank-name 500 fix +
non-vacuous AC1/4/5/6 tests). Record: [`tasks/067-paste-text-capture/TASK.md`](./067-paste-text-capture/TASK.md).
