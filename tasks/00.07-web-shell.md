---
id: "0.7"
title: Web app shell
phase: "0 — Foundation"
status: done
depends: [0.1, 0.6]
---

React SPA shell: React Router, authenticated layout (sidebar nav + topbar), login and first-run pages, TanStack Query client, typed API client built on shared Zod schemas. Tailwind CSS configured.

## Acceptance criteria
- [x] Unauthenticated users are redirected to login; session persists on refresh
- [x] Sidebar navigation stubs exist for all main sections
- [x] API errors surface as toasts, not blank screens
