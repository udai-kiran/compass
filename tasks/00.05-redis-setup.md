---
id: "0.5"
title: Redis client + BullMQ job foundation
phase: "0 — Foundation"
status: done
depends: [0.2]
---

Connect to Redis via `REDIS_URL` (192.168.2.196) with ioredis. Set up BullMQ: one queue + worker skeleton and a repeatable-job registration pattern (later used by recurring transactions, reminders, net-worth snapshots). Redis also backs sessions (task 0.6) and caching.

## Acceptance criteria
- [x] Redis connectivity reported in `/health`
- [x] A demo repeatable job runs and logs via BullMQ worker
- [x] Graceful worker shutdown; jobs survive API restarts
