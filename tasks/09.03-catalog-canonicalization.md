---
id: "9.3"
title: Catalog canonicalization + unit normalization
phase: "9 — Shopping core"
release: "2.3.0"
status: done
depends: [9.2]
---

`catalog_items` gives an item one canonical identity and a normalized unit, so "atta 5kg" and "wheat flour 5kg" resolve to the same thing and can be price-compared honestly.

Adds `unitPricePaise` to `packages/shared/src/money.ts` alongside `rupeesToPaise`/`formatINR`, plus unit conversion (g↔kg, ml↔L, piece counts) in integer arithmetic — no float rupees, and no float grams that round badly at scale.

**An ambiguous match is a review step, never a silent guess** — the same discipline as `matchAccount`/`matchCategory` in the extractor, where only a unique hit wins.

## Acceptance criteria
- [x] `unitPricePaise` computes ₹/kg and ₹/L correctly across mixed units, in integer math
- [x] A unique match auto-links; an ambiguous match is surfaced for review and never auto-resolved
- [x] Unmatched items remain usable rather than blocking the list
- [x] Canonicalization never invents a catalog entry without user confirmation
- [x] typecheck + lint + test green

**Done.** Committed `40921c1` on `feat/shopping-core-capture`. Owner-scoped catalog CRUD +
case-insensitive unique-or-ambiguous matching (canonicalizeItem auto-links only on a unique hit under
list+item row locks) + integer/BigInt `unitPricePaise` + exact decimal-string `convertToBaseQuantity`.
Codex code review (review-3) clean; iteration-2 strengthened AC2/3/4 test proofs. No migration.
Record: [`tasks/066-catalog-canonicalization/TASK.md`](./066-catalog-canonicalization/TASK.md).
