---
id: "0.3"
title: Fastify API skeleton
phase: "0 — Foundation"
status: done
depends: [0.2]
---

Fastify server with Zod type-provider, global error handler, request logging (pino), `/health` endpoint (checks Postgres + Redis connectivity), and layered structure: routes → services → repositories.

## Acceptance criteria
- [x] `GET /health` returns `{ ok, postgres, redis }` status
- [x] Invalid request bodies return structured 400s from Zod schemas
- [x] Route/service/repo layering established with one example resource
