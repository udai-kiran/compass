---
id: "1.6"
title: Split transactions
phase: "1 — Core ledger"
status: done
depends: [1.3]
---

`transaction_splits` table; a parent transaction's amount can be split across categories. Split editor UI with remainder math.

## Acceptance criteria
- [x] Splits must sum to parent amount (DB-level check)
- [x] Budgets/reports count splits, not the parent's single category
- [x] UI prevents saving unbalanced splits
