---
id: "1.8"
title: Migrate system module
phase: "1 — Module migration"
release: "2.0.0"
status: todo
depends: [1.1]
---

Routes: health, auth, profile, notifications, backup. Tables: users, user_profiles, family_members, projection_settings, notification_prefs, notifications, alert_ledger.

The riskiest module to move because it holds the cross-cutting guards. `plugins/auth.ts` and `plugins/security.ts` stay app-level (they must apply to every module), but their interaction with per-module plugin encapsulation needs explicit verification — Fastify hook scoping is exactly what changes when routes move into encapsulated plugins.

`/health` is the one non-`/api` public route and must stay public.

## Acceptance criteria
- [ ] Route snapshot unchanged; no migration diff; `backup.test.ts` green
- [ ] The global auth `onRequest` guard still applies to every route in every module
- [ ] `config: { public: true }` still works; `/health` still reachable unauthenticated
- [ ] Demo read-only chokepoint still 403s a mutating method in **every** module
- [ ] Security headers, Origin CSRF, and all three rate-limit buckets still applied globally
- [ ] typecheck + lint + test green
