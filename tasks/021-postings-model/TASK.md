# Task: 2.1 Postings model & balance invariant

## Status
IMPLEMENTING (dual-write strategy — see `PLAN-dualwrite.md`, which SUPERSEDES the atomic SP0/SP1/SP2 plan recorded below).
- SP0 (pure posting builders + `SafePaiseSchema`) MERGED → tag v2.1.0 (9130b85).
- PR-A (additive migration 0067, 4 system accounts incl. Clearing, full dual-write writer graph, per-transaction invariant, restore compat) MERGED → tag v2.2.0 (a77f1ce).
- The atomic branch `feat/postings-model-sp1` (which DROPPED legacy columns) is ABANDONED — it never produced a green mergeable intermediate. The dual-write approach keeps all legacy columns and converts readers PR-by-PR (PR-A→PR-G), each green + releasable.
- PR-B (balance readers → postings; `PLAN-pr-b.md`) COMPLETE in the working tree (NOT yet committed — git/PR/release gated on user). Codex review-19 approved plan; review-20 raised 6 blocking findings (fixed iter 2); review-21 APPROVED. Independent verify green (925/924 pass/0 fail/1 pre-existing skip; parity 7/7 stable ×3). Converted balances.ts, accounts.ts (accountBalancesAtDate+listAccounts), average-balance.ts to postings + opening-column addend + 500-class overflow guards; new postings-balance-parity.test.ts. NEXT: PR-C (lib/periods.ts income/expense/spend → postings).
- NOTE: the "SP1 execution log" and "SP1"/D23 sections below describe the ABANDONED atomic plan; retained only for history. The live plan is `PLAN-dualwrite.md` + per-PR PLAN-pr-*.md files.

## SP1 execution log (HISTORICAL — abandoned atomic plan; superseded by PLAN-dualwrite.md)
- Baseline (pre-cutover, state-1.md): apps/api 903 tests → 902 pass / 0 fail / 1 skip (exit 0). apps/extractor 63 → 62 pass / 1 fail (PRE-EXISTING: statement-duplicate.test.ts throws when DATABASE_URL unset; unrelated). Latest migration 0066_eager_spectrum. apps/api DB-backed tests pass ⇒ live Postgres reachable for verification.
- SP1 decomposed into slices B1→B4 (backend) + F1 (frontend) on ONE branch; converge to green only after B4/F1. See DELEGATION.md.
- SP0-era report files: backend-1.md, verification-1.md, git-facts.md. SP1 uses backend-2.md onward.
- B1a DONE (backend-2.md): schema edits + migration 0067_slim_blizzard.sql. Coordinator-verified SQL correct: postings table (tx_id cascade FK, account_id/category_id FK, note default ''), account_system_kind enum, accounts.system_kind + unique partial idx accounts_system_kind_idx (user_id,system_kind) WHERE not null, 'system' added to account_type, dropped opening_balance_paise + transactions.{account_id,amount_paise,category_id,necessity,is_opening} + 2 indexes. Barrel exports postings exactly once (via shared/ledger star; module named re-export excludes it). First backend-engineer attempt died mid-run leaving partial hubs/ledger edits (reverted); B1a split smaller to fit run budget.
- B1b DONE (backend-3.md): post-entry.ts (postEntry/replacePostings/updateTransactionHeader/seedSystemAccounts/resolveSystemAccounts), backup.ts registration (verified correct), auth.ts seeding call. KNOWN DEFECTS to fix before green: (1) post-entry.ts line 2 imports `Db` unused → noUnusedLocals/lint fail — remove it. (2) seedSystemAccounts onConflictDoNothing targets the PARTIAL unique index accounts_system_kind_idx but omits the predicate; Postgres needs `targetWhere: sql\`system_kind is not null\`` to infer a partial-index arbiter or it errors at plan time — add targetWhere (import sql), or switch to select-then-insert. Fold these fixes into a later backend slice.
- REMAINING: B2 write-path, B3 read-path, B4 shared+backup/restore+extractor+fixtures, F1 web. backend-engineer runs are long and one died at ~7-file scope; keep each sub-slice ~3-5 files.
- BFix DONE (backend-4.md, coordinator-verified in source): post-entry.ts line 2 unused `Db` import removed (`import type { DbOrTx }`); `seedSystemAccounts.onConflictDoNothing` now carries `targetWhere: sql\`system_kind is not null\`` (sql added to drizzle import) so the partial unique index `accounts_system_kind_idx` is a valid arbiter. Both confirmed by reading post-entry.ts:1-2,157.
- B4s DONE (backend-5.md; verification-2.md; coordinator-verified in source + isolation-green): `packages/shared/src/schemas/ledger.ts` per D23 — imported SafePaiseSchema; dropped `openingBalancePaise` from `AccountSchema` RESPONSE (kept on CreateAccount/UpdateAccount INPUT as SafePaiseSchema.default(0)/.optional()); `TransferResultSchema` → `z.object({ transactionId: z.uuid() })`; SafePaiseSchema adopted on 15 money fields (Split/Transaction/AccountWithBalance.balance/TransactionPage totals/SetSplits/TransferSuggestion/Create+UpdateTransaction.amount[refine !=0]/Create+Epf amount[refine>0]/EpfResult). Deliberately LEFT unchanged (documented, defensible; NOT SafePaiseSchema): coerced TransactionFilter min/max (need coercion), and the peripheral non-posting money fields requiredAmbPaise/overdraft/AMB (out of SP1 blast radius). `transferLinkId`/`transferCounterpartAccountId` schema unchanged (repurposed in hydrate, not the schema). ISOLATED VERIFICATION (packages/shared is self-contained, compiles even while the wider API tree is intentionally broken): `npm run typecheck -w packages/shared` exit 0; `npm run test -w packages/shared` 212 pass / 0 fail / 0 skip, exit 0. ledger.test.ts unchanged and still passes (CreateTransaction still keeps accountId/date/amountPaise per D23).
- NOTE: landing B4s WIDENS the (expected) compile breakage across API+web consumers (they still reference the removed `AccountSchema.openingBalancePaise` and old `TransferResult` shape). This is correct cutover order — the shared keystone lands first, then B2/B3/B4/F1 converge the consumers. Tree intentionally stays red until convergence.
- STILL PENDING (tree red until all land, then converge): B2a–B2e write-path, B3a–B3d read-path, B4 backup/restore/db-restore/extractor/fixtures, F1 web. THEN single convergence verification (db:generate one 0067, live db:migrate, typecheck all 6 ws, lint, test api+extractor, T6/T7 parity), THEN Codex implementation review(s). Codex has NOT yet reviewed the SP1 implementation (only the plan, review-1..4).

## D23 (lead-engineer implementation decision under D1/D2 — transfer/DTO field shape; NOT yet implemented)
The shared `TransactionSchema` (packages/shared/src/schemas/ledger.ts:378) must be finalized BEFORE hydrate/writers (everything types against it; changing it breaks API+web typecheck together — expected mid-cutover). Decided shape (minimizes web churn, faithful to D1 "one header, two real postings, no synthetic legs"):
- Keep `accountId: uuid` and `amountPaise: int` REQUIRED = the PRIMARY real leg. Ordinary/split/opening → the single real posting. Transfer → the OUTFLOW (negative) leg: accountId = from-account, amountPaise = -X.
- Keep `transferCounterpartAccountId: uuid|null` = the destination real account for transfers (the second real posting's account); null otherwise. This already exists and TransactionsPage/AccountLedgerPage read it.
- `transferLinkId` (currently uuid|null, driven by transfer_links which is being emptied): REPURPOSE as a non-null transfer MARKER — set it to the transaction's own id when classifyShape==='transfer', else null. (Avoids renaming the field across web; a follow-up 2.2 can rename to `isTransfer`.) hydrate derives transfer-ness from postings (two real, no system), NOT from transfer_links.
- `categoryId`/`necessity` ← counter posting (projectCounter) for ordinary; null for transfer/opening. `splits` ← projectSplits (negated). Opening transactions recognized by an Opening Balances posting and hidden from generic lists (D7) via a NOT EXISTS (postings join system opening account) predicate in filterWhere.
- `CreateTransaction`/`UpdateTransaction` INPUT keep accountId/amountPaise/categoryId/necessity (ordinary simple API); createTransaction builds ordinary/split postings via buildOrdinaryPostings/buildSplitPostings + resolveSystemAccounts, routes through postEntry. Transfer creation uses the separate transfer route/DTO (buildTransferPostings), NOT CreateTransaction.
- `AccountSchema`: drop `openingBalancePaise` from RESPONSE; keep `openingBalancePaise` as optional INPUT on `CreateAccount`/`UpdateAccount` (expands to an Opening Balances posting for ALL account types via buildOpeningPostings + postEntry). `SplitSchema` unchanged (id/categoryId/amountPaise/note).
- Money fields adopt `SafePaiseSchema` (D22). Flip account-balances.test.ts unsafe fixture to expect refusal.
Open sub-choice deferred to implementer with this default: repurpose `transferLinkId` marker as above (chosen) vs add `isTransfer` boolean (rejected: more web churn now).

## Precise remaining slice plan (each = one backend-<n>/frontend-<n> engineer run, ~3-5 files, tree stays broken until the end)
- BFix: fix the two post-entry.ts defects (unused `Db` import; seedSystemAccounts partial-index arbiter → add `targetWhere: sql\`system_kind is not null\``). Can be folded into B4s or done standalone.
- B4s (SHARED CONTRACT — do EARLY so later slices type against final shape): packages/shared/src/schemas/ledger.ts per D23 + ledger.test.ts. This is the keystone; land it before B2a.
- B2a: transactions.ts — hydrate (postings→DTO projection incl. systemKindOf via resolveSystemAccounts/account-type map), filterWhere (drop account/amount header cols; opening-hide + transfer via postings EXISTS/joins; keep min/max amount + accountId filters by joining the real posting), listTransactions totals (sum real-posting of non-transfer non-opening), createTransaction/updateTransaction/setSplits/bulk → postEntry/replacePostings. Retire isTransferSql (transfer_links) → posting-shape SQL.
- B2b: accounts.ts + reconciliation-writes.ts — createAccount opening expansion (all types), updateAccount opening change (replace planOpeningBalanceChange, find opening txn by Opening Balances posting), deleteAccount used-check via postings, listAccounts/accountBalancesAtDate exclude system + narrow type (D17), assertOwnedRealAccount rejects system; absorbCarryover adjusts opening posting idempotently preserving lock order (D11).
- B2c: transfers.ts — createTransfer (one header, buildTransferPostings, postEntry), linkTransfer MERGE (D16: outflow header survives, migrate attachments+transaction_links, reject if either txn has sipId/reconciledStatementId/policyId/resourceId/recurringTemplateId), unlinkTransfer split-back (recreate ordinary + system posting each), suggestTransfers/autoLinkTransfers rewrite.
- B2d: recurring.ts (EMI two posting-backed txns, D21), epf-contributions.ts, insurance.ts, categories.ts (merge → posting categories), merchants.ts (header-only via updateTransactionHeader).
- B2e: imports.ts, review-actions.ts, transfer-classification.ts (one surviving transfer id on both drafts; card-repay reuse = merge), review-queue.ts, import-reconciliation.ts, sip-lifecycle.ts (header-only audit; convert any predicate reading removed cols to postings), demo.ts, db/seed.ts (seed system accounts + posting-based data).
- B3a: lib/periods.ts (incomeExpense D4 liability-inflow exclusion via real postings joined accounts.type + exclude transfer/opening shapes; spentByCategory positive Expenses postings D5; spendByNecessity necessity-on-posting + tenant-scoped category join; count(distinct transaction_id) D6).
- B3b: balances.ts, average-balance.ts, cards.ts, emis.ts, reconciliation-reads.ts (balance = sum(postings) on real accounts; range-check aggregates before Number()).
- B3c: dashboard.ts, reports.ts, insights.ts, cashflow.ts, bills.ts, goals.ts (raw-SQL aggregations → postings; cardinality D6).
- B3d: search.ts, user-tasks.ts, prefs.ts (large-txn: single real posting per txn, exclude opening+transfer → ZERO alerts for transfer, D20), categorize.ts, tools.ts, sip-installments.ts, networth.ts.
- B4: backup.ts CSV projection from postings + JSON archive posting rows, restore-user.ts (D19 remap algorithm: fresh-guard ignores system_kind, regen system accounts, old→new system id map by kind, rewrite posting account_id), db/restore.ts ordering, apps/extractor/src/db.ts raw SQL, ALL fixtures across api+extractor.
- F1 (frontend-engineer): TransactionsPage.tsx, AccountLedgerPage.tsx, TransactionDrawer.tsx, CardDetailPage.tsx (opening balance surface), account create/edit form, apps/web/src/lib/queries.ts (suggest/link/unlink/record-transfer + AccountSchema opening input).
- CONVERGE: run `npm run db:migrate` (T1, live DB), `npm run typecheck` (T2, all 6 ws), `npm run lint` (T3), `npm run test` api+extractor (T4) via a SEPARATE verification worker; then Codex implementation review(s); then prove T6(a)-(o) + T7(a)-(c) parity fixtures recording before/after numbers.

## Progress
- **SP0 DONE (uncommitted).** Pure additive foundation landed: `apps/api/src/modules/ledger/services/postings.ts` (PostingDraft/SystemKind; assertSafePaise/sumPaise/assertZeroSum in BigInt; buildOrdinary/Split/Transfer/Opening; classifyShape/projectRealLeg/projectCounter/projectSplits) + `postings.test.ts` (17 tests, seeded property loop + boundary + round-trip) + `SafePaiseSchema` added additively in `packages/shared/src/money.ts`. Zero behavior change; nothing wired to it yet. Verification (verification-1.md): only those 3 files changed; typecheck 0, lint 0, targeted 17/17, full apps/api 902 pass/0 fail/1 pre-existing skip. Codex review-4: CORRECT-AND-BEHAVIOR-NEUTRAL, no issues.
- **SP1 NEXT** — the atomic cutover (one `0067_*` migration + system accounts + all read/write conversion + shared contract + web + backup/restore + extractor). Large (~40 files); to be delegated + verified + Codex-reviewed as its own unit per D1–D22.

## Objective
Replace single-entry (`transactions.accountId`+`amountPaise`; balance = `accounts.openingBalancePaise + sum(amountPaise)`) with double-entry postings: a `postings` table carries the money, `transactions` becomes a header, every transaction's postings sum to exactly zero, and an account's balance is purely `sum(postings)`. System accounts (`Expenses`, `Income`, `Opening Balances`) are seeded per user. Category + necessity move onto the posting. Integer paise throughout, provably.

Roadmap: `tasks/02.01-postings-model.md`. Depends on 1.9 (done, v2.1.0). Blocks 2.2–2.7.

## Governing constraint (root cause of the sequencing)
1. **Zero-sum per transaction** forces the transfer/split remodel: a transfer becomes ONE transaction with two non-system-account postings (not two independently-balanced rows); a split becomes multiple `Expenses` postings on one transaction. This is the functional remodel the roadmap filed under 2.2/2.3.
2. **No dual-write / no backfill** (prod DB recreated from scratch). Removing the header money columns breaks ~45 ORM compile sites + every raw-SQL aggregation at once (`investigation-1.md` §3). Write path, read/aggregation path, column removal, backup, and shared contract must land in ONE atomic changeset.

## ROADMAP RESCOPE (record + surface to user)
This task provably absorbs: the functional transfer remodel (**2.2**), the split→postings fold (**2.3**), the bulk of consumer conversion (**2.4**), and the shaping of the simple/multi-leg transaction API (**2.5**). After 2.1: **2.2** = drop the (empty) `transfer_links` table + its dead exclusion code; **2.3** = drop the (empty) `transaction_splits` table + `SplitSchema` cleanup; **2.4** = residual consumer polish; **2.5** = formalize/extend the multi-leg API. Their task files' descriptions + `depends` need updating. Flagged to the user before editing those product docs.

## Resolved design decisions (from review-1)
- **D1 Transfer identity.** Remodel transfers in 2.1 as ONE transaction with two non-system-account postings. Change the transfer DTO (`CreateTransfer`/`TransferResult`) and the transfer-aware web consumers (`TransactionsPage.tsx`, `AccountLedgerPage.tsx`). NO synthetic two-leg projection. Retire/rewrite `linkTransfer`/`autoLinkTransfers`/`unlinkTransfer`/`suggestTransfers`.
- **D2 Ordinary-txn DTO projection (keeps web mostly untouched for non-transfers).** `hydrate()` reconstructs the legacy `Transaction` DTO: `accountId`/`amountPaise` ← the single non-system posting; `categoryId`/`necessity` ← the system counter-posting; `splits` ← the N `Expenses` postings with **amounts negated** to legacy signed semantics and `note` from the posting. Opening transactions recognized by their `Opening Balances` posting.
- **D3 Split note.** Add `note text NOT NULL DEFAULT ''` to `postings` for lossless split reconstruction.
- **D4 Liability-inflow semantics (parity-critical).** Income/expense reporting MUST preserve today's rule: negative real-posting = expense; positive real-posting = income ONLY when the real account is non-liability (`LIABILITY_ACCOUNT_TYPES`); opening + transfer transactions excluded. Implement by aggregating postings on REAL (non-system) accounts joined to `accounts.type`, excluding transfer-shaped transactions (two real-account postings / no system posting) and opening transactions (have an `Opening Balances` posting). "Sum the Income system account" is explicitly NOT the reported-income definition.
- **D5 Spend sign/category/necessity.** `spentByCategory` sums POSITIVE `Expenses`-account postings grouped by category. Necessity is copied onto each posting at write time (tx override onto every split posting); category joins stay tenant-scoped by joining the parent transaction's `userId`.
- **D6 Cardinality.** Merchant/biggest-spend/count/report queries use `count(distinct transaction_id)` while summing posting amounts, so a split transaction isn't counted N times.
- **D7 Opening-row visibility.** Opening transactions stay hidden from generic transaction lists/counts/search by detecting the `Opening Balances` posting (replacing the `is_opening` filter). Newly-created opening transactions for card/loan/investment types are hidden the same way — no new visible rows vs. today.
- **D8 Scoping/backup.** `postings` scopes via parent transaction: register in `ALL_TABLES` + `LINKED_TABLES` (`{ fk: "transaction_id", parent: "transactions" }`), NO `postings.user_id`, positioned in `ALL_TABLES` immediately AFTER both `accounts` and `transactions` so both restore paths satisfy FKs. Two DISTINCT surfaces, not conflated: (i) the JSON/encrypted archive round-trips `postings` ROWS (export + restore, with D19 system-account remap); (ii) the human-readable transactions CSV (`backup.ts:121`, export-only, NOT a restore format) is rewritten to derive amount/account/category from postings.
- **D9 System accounts.** Add a dedicated `"system"` value to the account-type pg enum + `system_kind` enum column {`expenses`,`income`,`opening`} nullable; UNIQUE partial index `(user_id, system_kind) WHERE system_kind IS NOT NULL`. Excluded from EVERY generic account query (`listAccounts`, `accountBalancesAtDate`/net worth, bank/cash, AMB, cards, `deleteAccount` used-check). `assertOwnedAccount` (or a new `assertOwnedRealAccount`) REJECTS system-account IDs supplied through the simple transaction API. Guard edit/delete/archive. Idempotent seeding at registration + demo + restore. Restore REGENERATES system accounts and remaps postings (never restores archived system-account IDs).
- **D10 Opening-balance handling.** Replace `planOpeningBalanceChange` with a posting-based equivalent: find the account's opening transaction via its `Opening Balances` posting; update/insert/soft-delete the whole transaction; preserve the effective-date rule (dated before the earliest ordinary transaction); type change no longer moves storage (all types unified). `createAccount` with an `openingBalancePaise` INPUT expands to an opening transaction for ALL types.
- **D11 Reconciliation.** `absorbCarryover` adjusts the canonical opening transaction's postings (card posting −D, `Opening Balances` +D), idempotent (re-derive, apply only remaining delta), preserving the existing account-row lock order and the `Number.isSafeInteger` refusal.
- **D12 Integer-paise safety (AC6).** `assertZeroSum` computes in `BigInt`. Money input schemas use safe-integer validation (`.refine(Number.isSafeInteger)`/`z.number().int()` + safe check). `isSafeInteger` checks after every arithmetic step deriving postings; range-check SQL aggregates before `Number(...)`. Property test generates values near 0 AND near ±`Number.MAX_SAFE_INTEGER`. NEVER proportionally rescale splits — require an exact new allocation or an explicit residual-leg adjustment.
- **D13 Transactional enforcement (AC2).** A single invariant-enforcing writer helper (`postEntry`) wraps header+postings in ONE DB transaction and asserts zero-sum immediately before persistence. EVERY writer routes through it (create, update, bulk, transfer, opening, import commit, recurring fire, review-actions, insurance premium, EPF, demo seed, reconciliation). No posting write exists outside it.
- **D14 SP0 is pure TS.** SP0 = posting-shape helpers + property tests ONLY, no schema/seed change. All schema + system-account seeding + conversions + shared contract + web land atomically in SP1 as a single `0067_*` migration.
- **D15 updateTransaction round-trips.** Separate header patches from posting patches: changing `accountId` moves the real posting; changing `amountPaise` updates both sides preserving exact negation; changing category/necessity updates the system posting(s); changing a split-transaction amount uses a defined policy (reject until splits resubmitted / explicit residual) — never silent rescale. Bulk category actions update all applicable `Expenses`/`Income` postings, never touching transfers/openings.

## Resolved design decisions (from review-2)
- **D16 Transfer merge / unlink identity.** Fresh `createTransfer` and inbox auto-classification produce ONE transfer transaction directly (two non-system postings). `linkTransfer(outTxId, inTxId)` of two PRE-EXISTING ordinary transactions is a MERGE: keep the outflow transaction as the surviving header; migrate the counterpart's children (attachments, `transaction_links`) onto the survivor; replace both transactions' system postings with the two real postings on the survivor; delete the counterpart header. `unlinkTransfer` splits the transfer header back into two ordinary transactions, recreating an `Expenses`/`Income` system posting on each per account-type/sign rules. Card-repayment acceptance (`transfer-classification.ts:233,248`) that reuses an existing paying-side transaction must produce a single transfer header via the same merge, not a second row. Extracted drafts that today store two distinct transaction IDs (`transfer-classification.ts:114,120`) store the one surviving transfer id on both sides. Scope adds web `TransactionDrawer.tsx` and `apps/web/src/lib/queries.ts` (suggestions/link/unlink/record-transfer contracts).
  - **Reference policy (deterministic, prevents dangling refs / silent loss):** a transfer leg is a plain money move, so `linkTransfer` REJECTS (HttpError) if EITHER transaction carries any header-level domain reference — `sipId`, `reconciledStatementId`, `policyId`, `resourceId`, or `recurringTemplateId`. Only child collections migrate to the survivor: `attachments` and `transaction_links`. Consequently the merged transfer header carries none of those domain references, and `unlinkTransfer`'s two recreated ordinary transactions carry none either (attachments/links remain on the surviving/outflow transaction; the recreated inflow starts clean). This keeps merge/unlink total and reversible without redesigning SIP/reconciliation/insurance/resource/recurring linkage.
- **D17 `"system"` account type is DB-INTERNAL.** Add `"system"` ONLY to the Postgres account-type enum; do NOT add it to the public `AccountTypeSchema`. Every generic account query already filters system accounts out (D9), so the existing `as AccountType` projections (e.g. `accounts.ts:173`) only ever see real types. Add a narrowing guard at each DB→public boundary that asserts/filters `system_kind IS NULL` before casting, so a system row can never reach `ACCOUNT_BUCKET`/`ACCOUNT_RETURN_BPS`/web labels or the net-worth exhaustiveness test. No change to `networth.ts`, `goal-returns.ts`, `account-meta.ts`, or `networth.test.ts` enum maps.
- **D18 Writer audit: posting-affecting vs header-only.** `postEntry` governs every INSERT/DELETE or monetary/account/category/necessity mutation of postings. Header-only metadata/FK updates may use a clearly-named header-only helper and must NOT touch postings, but every transaction-mutating file is audited and listed: header-only writers `merchants.ts:57` (merchant rename), `sip-lifecycle.ts:501,515` (sip linkage FK), `reconciliation-writes.ts:129,140` (`reconciledStatementId`). Confirm `sip-lifecycle.ts` predicates don't rely on removed `transactions.accountId/amountPaise` columns; if they do, convert those reads to postings.
- **D19 Restore algorithm (user archive).** Concrete sequence: (1) fresh-account precondition guard ignores `system_kind IS NOT NULL`; (2) delete/retain seeded system accounts deliberately (reverse-order delete must skip them); (3) regenerate exactly one system account per kind for the target user; (4) build `old archived system-account id → new account id` keyed by `system_kind`; (5) rewrite each restored posting's `account_id` through that map before insert; (6) restore real accounts + transactions before postings; (7) never insert archived system-account rows as ordinary accounts. Since postings have no `user_id` (D8), this remap can't ride the generic `restore-user.ts:122` row rewrite — it needs an explicit posting-account remap pass.
- **D20 Transaction-level (non-aggregate) consumers keep per-transaction real-posting semantics.** `prefs.ts` large-transaction alerts (`prefs.ts:85-99`: one alert per transaction, signed real amount, account filter, exclude opening/transfer) select the single real posting per transaction — never sum system postings — so a split yields exactly ONE alert and a transfer yields ZERO alerts (transfers are excluded outright, `prefs.ts:99`; the two real postings must never be counted). Same rule for search/list/detail row-level consumers.
- **D21 Recurring EMI shape (parity-critical).** `recurring.ts` EMI materialization stays TWO separate posting-backed transactions — a negative source-account entry (counts as expense) and a positive liability-account principal entry (counts as neither income nor expense) — each balanced by its own system posting. They must NOT be consolidated into one two-real-posting transfer just because two real accounts are involved. Add to T6.
- **D22 One reusable `SafePaiseSchema`.** Define a single `z.number().int()` + `Number.isSafeInteger` refinement in `@compass/shared` and use it for every money field, rather than per-author refinements. Flip the deliberately-unsafe fixture `account-balances.test.ts:16,27` (`9007199254740993`) from "accepts rounded output" to "asserts overflow refusal."

## Scope (files)
Schema: `db/shared/ledger.ts` (transactions header; add `postings` beside it; drop `is_opening`), `db/shared/hubs.ts` (drop `accounts.opening_balance_paise`; add `system_kind`; add `"system"` to account-type enum), `modules/ledger/schema.ts` (re-export `postings` only — do NOT redefine; per CLAUDE.md:49), `db/schema.ts` barrel (export once), drizzle `0067_*`.
Write path: `modules/ledger/services/{transactions,transfers,accounts,recurring,epf-contributions,categories,merchants}.ts`, `modules/investments/services/sip-lifecycle.ts` (header-only audit per D18), `modules/ingest/services/{imports,review-actions,transfer-classification,review-queue,import-reconciliation}.ts`, `modules/protection/services/insurance.ts`, `modules/system/services/{demo,auth}.ts`, `db/seed.ts`.
Read/aggregation: `lib/periods.ts`, `modules/ledger/services/{balances,accounts,average-balance,search,user-tasks}.ts`, `modules/planning/services/{cashflow,dashboard,reports,insights,goals,bills}.ts`, `modules/credit/services/{cards,emis,reconciliation-reads,reconciliation-writes}.ts`, `modules/automation/services/{categorize,tools}.ts`, `modules/investments/services/{sip-installments,networth}.ts`, `modules/system/services/prefs.ts`.
Backup/restore: `modules/system/services/{backup,restore-user}.ts` (+ `backup.test.ts`), `db/restore.ts`.
Extractor (OUTSIDE apps/api — workspace typecheck + extractor tests must cover): `apps/extractor/src/db.ts` raw SQL reading `transactions.amount_paise`/`account_id`.
Shared: `packages/shared/src/schemas/ledger.ts` (+ `.test.ts`) — keep ordinary `Transaction` DTO via projection; add `note` to posting shape; change transfer DTO; drop `Account.openingBalancePaise` from response, keep as `CreateAccount` INPUT.
Web (limited): account opening-balance surface (`CardDetailPage.tsx:184`, account create/edit form) + transfer-aware views (`TransactionsPage.tsx`, `AccountLedgerPage.tsx`, `TransactionDrawer.tsx` — the interactive link/suggest/unlink UI) + `apps/web/src/lib/queries.ts` (suggestions/link/unlink/record-transfer contracts).
Tests/fixtures: convert every API + extractor test that inserts legacy `transactions.accountId/amountPaise/isOpening` or `accounts.openingBalancePaise`.

## Plan (sub-steps)
- **SP0** — pure TS: `postEntry` posting-shape builders + `assertZeroSum` (BigInt) + posting→DTO projection helpers + **property tests** (boundary values). No schema/seed/behavior change. Ships green independently.
- **SP1** — atomic cutover (one `0067_*` migration): add `postings`+`system_kind`+`"system"` type + unique index; seed system accounts (registration/demo/restore, idempotent); route ALL writers through `postEntry`; rewrite `hydrate()` projection; convert EVERY read/aggregation consumer (preserving D4 liability semantics, D6 cardinality, D7 opening hiding); posting-based opening-balance + reconciliation; drop header money columns + `accounts.opening_balance_paise`; shared contract + web; backup/restore incl. CSV + ordering; extractor SQL; convert all fixtures. `transfer_links`/`transaction_splits` left empty-but-present (all writers converted → guaranteed empty). Typecheck/lint/test green.
- **SP2** — parity verification (see below) + full suite.

## Acceptance Criteria (roadmap 2.1)
- AC1 `postings` table; `transactions` header with no account/amount. AC2 zero-sum enforced on write (transactional, all writers) + property test. AC3 balance solely from postings; `openingBalancePaise`+`is_opening` removed. AC4 system accounts seeded per user; not deletable (edit/archive/delete/simple-API guards + unique index). AC5 category+necessity on posting. AC6 integer paise, BigInt invariant, safe-integer validation, no rescale. AC7 `postings` in `ALL_TABLES` + `LINKED_TABLES`, correct restore order. AC8 typecheck+lint+test green (incl. extractor workspace).

## Verification
- T1 `npm run db:generate` → exactly one new `0067_*` migration (SP1); review SQL; `db:migrate` clean.
- T2 `npm run typecheck` clean across ALL 6 workspaces (catches extractor). T3 `npm run lint` clean. T4 `npm run test` (api + extractor) green incl. property test + `backup.test.ts`.
- T5 property test: balanced sets accepted; any ±1 perturbation rejected; boundary values near ±`MAX_SAFE_INTEGER` handled via BigInt.
- T6 PARITY fixtures (record numbers before/after cutover): (a) positive card/loan txn NOT linked as transfer → neither income nor expense; (b) negative liability txn; (c) card refund; (d) linked bank→card transfer excluded; (e) split expense w/ per-split categories + tx-level necessity override; (f) opening balances for bank, card, loan, investment; (g) account type change with existing opening amount; (h) reconciliation carryover applied twice (idempotent); (i) top-merchant count with a split transaction; (j) transaction-list totals + filters; (k) account-ledger count/visibility; (l) large-transaction alert with a split (exactly one alert) and a transfer (ZERO alerts — excluded); (m) recurring EMI materialization → source entry = expense, principal entry = neither income nor expense (D21); (n) transfer link of two pre-existing rows then unlink (child remapping + split-back, D16); (o) extractor statement matching.
- T7 backup/restore, THREE distinct cases: (a) user archive JSON export→restore round-trip incl. postings + system-account ID remap (D19); (b) full-dump restore ordering (postings after accounts+transactions); (c) human-readable transactions CSV projection from postings.

## Non-Goals
Physically dropping `transfer_links` (2.2) / `transaction_splits` (2.3); DB-level invariant trigger (2.6); extending public multi-leg API (2.5); postings-native UI beyond the surfaces above (2.7).

## Review log
- review-1 (Codex, plan): BLOCKING — liability inflows, transfer identity, split note, SP0 non-neutral + migration-count, missing consumers (categories/review-queue/extractor/backup CSV/restore/import-reconciliation/deleteAccount), integer-paise, LINKED_TABLES, system-account guards+unique index, transactional enforcement, roadmap rescope. All folded into decisions D1–D15 above. → revised to iteration 2.
- review-2 (Codex, plan): core design blessed as implementation-ready; 10/12 prior items RESOLVED, 7 concrete amendments (transfer merge/unlink identity + web Drawer/queries; "system" enum public-vs-internal; user-restore remap algorithm; writer audit merchants/sip-lifecycle header-only; EMI + large-txn per-transaction semantics; three-way backup verification; reusable SafePaiseSchema). All folded into D16–D22, Scope, T6, T7. → revised to iteration 3.
- review-3 (Codex, plan): 5/7 amendments RESOLVED; two narrow blockers — (1) D16 must define handling of OTHER header references on the discarded transfer counterpart; (2) large-txn transfer parity must be unequivocally ZERO alerts. Deferred items (drop transfer_links/transaction_splits, DB trigger, multi-leg API, postings UI) confirmed as 2.2/2.3/2.5/2.6/2.7, not blockers here. Both blockers closed: D16 reference policy (reject transfer-link when either txn carries sipId/reconciledStatementId/policyId/resourceId/recurringTemplateId; only attachments+transaction_links migrate) and D20/T6(l) zero-alert wording. → APPROVED.
