---
id: "2.1"
title: CSV import staging pipeline
phase: "2 — Import & rules"
status: done
depends: [1.3]
---

Upload CSV → parse (streaming, handles large files) → stage rows into `imports` + `import_rows` tables. Nothing touches `transactions` until commit.

## Acceptance criteria
- [x] 50k-row CSV imports without blocking the event loop
- [x] Malformed rows collected and reported, not fatal
- [x] Batch metadata: source account, filename, row counts
