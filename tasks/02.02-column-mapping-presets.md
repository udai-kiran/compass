---
id: "2.2"
title: Column mapping UI + bank presets
phase: "2 — Import & rules"
status: done
depends: [2.1]
---

Mapping UI (which column is date/amount/merchant/…), date-format and debit/credit-convention handling. Presets for common bank formats; save mapping per account for one-click reuse.

## Acceptance criteria
- [x] Mapping preview shows first 10 parsed rows live
- [x] Saved preset auto-applies on next import for that account
- [x] Handles single signed-amount and separate debit/credit column formats
