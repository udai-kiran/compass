---
id: "2.4"
title: Import preview, commit & rollback
phase: "2 — Import & rules"
status: done
depends: [2.2, 2.3]
---

Preview screen for staged rows (dupes flagged, categories pre-filled by rules), per-row include/exclude, then atomic commit. Rollback deletes a committed batch's transactions.

## Acceptance criteria
- [x] Commit is a single DB transaction
- [x] Rollback removes exactly the batch's transactions and restores balances
- [x] Import history screen lists batches with commit/rollback actions
