- [P2] [schema.decomposition.test.ts:110](/home/udai/PennyPilot/apps/api/src/db/schema.decomposition.test.ts:110) does not actually detect duplicate table or enum objects. `tables` and `enums` contain unique ESM export names from `Object.entries(barrel)`, so the `Set` assertions at lines 110–114 are tautological. The identity assertions at lines 179–185 and 228–233 only compare each expected barrel export with its designated home; a second `pgTable("same_name", …)` or `pgEnum("same_name", …)` elsewhere—especially if unexported—would pass. Even two barrel aliases referencing the same object are treated as distinct tables until the count happens to fail. Thus the test does cover the expected 50 tables, `users`, and 38 enums, but its explicit “no duplicates” guarantee is incomplete. It should compare object identities and Drizzle table/enum database names across all defining namespaces, or add the required repository-wide static definition/name uniqueness check.