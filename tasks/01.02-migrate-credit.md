---
id: "1.2"
title: Migrate credit module
phase: "1 — Module migration"
release: "2.0.0"
status: done
depends: [1.1]
---

Routes: cards (15 endpoints), emis, overdraft-details, bank-details (23 endpoints total across all 4 groups). Tables: card_details, card_issuer_settings, card_statements, reward_entries, statement_reconciliations, emi_details, bank_details, overdraft_details.

`cards.ts` was 1182 lines and `emis.ts` 493. Beyond the move, `cards.ts` was split along its natural seams — cycle math, statement reconciliation (itself split into reads/writes), reward ledger, alert evaluation — into 6 separately testable units, none over ~500 lines. `card-due-tasks.ts` and `card-statements.ts` (not named in this task's original prose, but owning in-scope behavior/table respectively) moved alongside. The cycle-boundary logic (`cardCycle`/`splitByCycle`) and a new reward earn-rate interface (`getCardEarnRate`/`earnedRewardPoints`, built test-first — no such interface existed before this task) are both exposed for later reuse — see `tasks/008-migrate-credit/TASK.md` for the full design, including a documented discrepancy against `tasks/10.06-reward-aware-checkout.md`'s description of the interface's future data source.

## Acceptance criteria
- [x] Route snapshot unchanged; no migration diff; `backup.test.ts` green
- [x] `cards.ts` decomposed; no single resulting service over ~500 lines
- [x] Card due-date and utilization alert evaluation still fire from the alerts worker
- [x] Reward earn-rate lookup exposed as a documented interface for later reuse
- [x] typecheck + lint + test green
