---
id: "0.4"
title: Postgres + Drizzle ORM setup
phase: "0 — Foundation"
status: done
depends: [0.2]
---

Connect to Postgres via `DATABASE_URL` (192.168.2.196) using Drizzle ORM + node-postgres pool. Migration workflow (`drizzle-kit`), initial `users` table, seed script with demo data. All money columns are `bigint` minor units (paise) — never floats.

## Acceptance criteria
- [x] `npm run db:migrate` applies migrations against the configured server
- [x] `npm run db:seed` loads demo data idempotently
- [x] Connection pool sized and closed gracefully on shutdown
