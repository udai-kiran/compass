---
id: "1.1"
title: Migrate ledger module
phase: "1 — Module migration"
release: "2.0.0"
status: todo
depends: [0.3]
---

First real migration — the largest domain, and the template for the remaining seven.

Routes: accounts, categories, transactions, transfers, transaction-links, attachments, recurring, rules, resources, search, user-tasks. Tables: accounts, categories, resources, transactions, transaction_splits, transfer_links, transaction_links, attachments, merchant_rules, recurring_templates, user_tasks. Heaviest services: `imports.ts` (878), `accounts.ts` (507), `transactions.ts` (441).

Schema slice → services moved under `modules/ledger/` → a single prefixed Fastify plugin replacing the flat registrations. Behaviour and URLs stay byte-identical; this is a move, not a rewrite.

## Acceptance criteria
- [ ] Route snapshot from 0.3 unchanged
- [ ] `npm run db:generate` produces no migration diff
- [ ] `backup.test.ts` green
- [ ] typecheck + lint + test green
- [ ] Demo-mode 403 on a mutating ledger route still enforced
