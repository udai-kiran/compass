# Independent verification — 014-migrate-planning, Slice 0 (schema-only, D1)

Read-only verification. No files edited by me except this report. No mutating git commands run
(no add/commit/push/stash/checkout).

## Files inspected
- `tasks/014-migrate-planning/TASK.md` (D1, AC2, AC3, AC10, AC12, AC13)
- `tasks/014-migrate-planning/DELEGATION.md` ("Iteration 1 — Slice 0")
- `CLAUDE.md`
- `apps/api/src/db/schema.ts`
- `apps/api/src/db/schema.smoke.test.ts`
- `apps/api/src/modules/planning/schema.ts`
- `apps/api/src/modules/planning/schema.smoke.test.ts` (new, untracked)
- `apps/api/src/modules/ledger/schema.ts`
- `apps/api/src/route-surface.snapshot.txt`, `apps/api/src/route-table.snapshot.txt` (hashed only)

## Files changed
None by me. Files already modified/added in the working tree at the start of this session (pre-existing,
attributed to the implementation step under review — not made by me):
- Modified: `CLAUDE.md`, `apps/api/src/db/schema.ts`, `apps/api/src/modules/ledger/schema.ts`,
  `apps/api/src/modules/planning/schema.ts`
- Untracked (new): `apps/api/src/modules/planning/schema.smoke.test.ts`
- Also untracked but unrelated to this slice (task-record files, not touched or evaluated further):
  `tasks/013-release-v1.97.0/commit-pr-final.md`, `tasks/014-migrate-planning/` task-record files
  (TASK.md, DELEGATION.md, investigation-1/2.md, review-1/2/3.md, backend-1.md — `implementation-1.md`
  called for by DELEGATION.md's own instructions was **not present** in the directory at verification
  time).

---

## 1. `git status --porcelain`

```
 M CLAUDE.md
 M apps/api/src/db/schema.ts
 M apps/api/src/modules/ledger/schema.ts
 M apps/api/src/modules/planning/schema.ts
?? apps/api/src/modules/planning/schema.smoke.test.ts
?? tasks/013-release-v1.97.0/commit-pr-final.md
?? tasks/014-migrate-planning/
```

## 2. Complete `git diff`

```diff
diff --git a/CLAUDE.md b/CLAUDE.md
index 00ccb50..b7b119a 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -46,7 +46,7 @@ node --test apps/api/src/services/capital-gains.test.ts
 - **`plugins/auth.ts`** — Redis-backed sessions (argon2 password hash, signed httpOnly SameSite=Lax cookie). It also holds the **single demo-mode chokepoint**: a demo session is rejected on any mutating HTTP method (`MUTATING_METHODS`), so seeded demo data is immutable and every new POST/PATCH/DELETE route is demo-safe automatically.
 - **`plugins/security.ts`** — hand-rolled (deliberately no `@fastify/helmet`/`rate-limit`/`csrf`): security headers, **CSRF via Origin check** on state-changing requests, and Redis fixed-window rate-limit buckets (`AUTH_BUCKET` etc.).
 - **Jobs:** BullMQ on Redis, started in `jobs/index.ts` (`startJobs`). Config is validated at boot in `config.ts` (Zod) — add new env vars there.
-- **Transitional module scaffold:** `modules/<domain>/` (`schema.ts`, `services/`, `routes/`, `plugin.ts`) is starting to replace the flat `services/x.ts`/`routes/x.ts` layout, one domain at a time (Phase 1 of the roadmap). `app.ts` registers a module's `plugin.ts`, not its routes directly. `db/schema.ts` stays the schema barrel — it re-exports each module's `schema.ts` — and both it and every `modules/<domain>/schema.ts` import shared identity tables from `db/core-schema.ts` (currently just `users`), a deliberately narrow, cycle-free leaf — **not** a general destination for every cross-module foreign key.
+- **Transitional module scaffold:** `modules/<domain>/` (`schema.ts`, `services/`, `routes/`, `plugin.ts`) is starting to replace the flat `services/x.ts`/`routes/x.ts` layout, one domain at a time (Phase 1 of the roadmap). `app.ts` registers a module's `plugin.ts`, not its routes directly. `db/schema.ts` holds every `pgTable()`/`pgEnum()` definition and is the single Drizzle Kit entry point; each `modules/<domain>/schema.ts` is a thin named re-export of that domain's tables, so module code imports tables from `../schema.ts` rather than reaching into the barrel. `db/schema.ts` imports shared identity tables from `db/core-schema.ts` (currently just `users`), a deliberately narrow, cycle-free leaf — **not** a general destination for every cross-module foreign key. Task 1.9 converts these thin re-export surfaces into physical per-module ownership.
 
 ### Money & domain rules
 - **Money is always integer paise** (minor units) end to end — never float rupees. Use `packages/shared/src/money.ts` (`rupeesToPaise`, `formatINR`, `standardEmiPaise`). Formatting is `en-IN` INR.
diff --git a/apps/api/src/db/schema.ts b/apps/api/src/db/schema.ts
index 3e77f46..bc74574 100644
--- a/apps/api/src/db/schema.ts
+++ b/apps/api/src/db/schema.ts
@@ -19,8 +19,6 @@ import {
 } from "drizzle-orm/pg-core";
 import { users } from "./core-schema.ts";
 export { users } from "./core-schema.ts";
-export * from "../modules/planning/schema.ts";
-
 /**
  * Schema conventions:
  * - ids: uuid, generated by Postgres
@@ -29,10 +27,11 @@ export * from "../modules/planning/schema.ts";
  * - timestamps: timestamptz, created_at/updated_at on every table
  * - soft delete / archive: *_at nullable timestamptz
  *
- * `users` lives in `./core-schema.ts` (a cycle-free leaf) and `projectionSettings`
- * lives in `../modules/planning/schema.ts` — both re-exported from this barrel.
- * See `modules/<domain>/` for the emerging module-scaffold convention that
- * later Phase-1 tasks will extend to the rest of these tables.
+ * `users` lives in `./core-schema.ts` (a cycle-free leaf), re-exported from this barrel.
+ * Every `pgTable()`/`pgEnum()` definition lives here — the single Drizzle Kit entry point.
+ * Each `modules/<domain>/schema.ts` is a thin named re-export of its domain's tables,
+ * so module code imports from `../schema.ts`. Task 1.9 converts these thin surfaces
+ * into physical ownership.
  */
 
 /** Per-user profile information. */
@@ -746,6 +745,17 @@ export const subscriptionDismissals = pgTable(
   (t) => [uniqueIndex("subscription_dismissals_unique_idx").on(t.userId, t.merchant)],
 );
 
+/** Per-user assumptions used only for forward-looking goal projections. */
+export const projectionSettings = pgTable("projection_settings", {
+  userId: uuid("user_id")
+    .primaryKey()
+    .references(() => users.id, { onDelete: "cascade" }),
+  /** Broad-equity annual return assumption (1200 = 12%). */
+  equityReturnBps: integer("equity_return_bps").notNull().default(1200),
+  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
+  updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
+});
+
 export const notificationPrefs = pgTable(
   "notification_prefs",
   {
diff --git a/apps/api/src/modules/ledger/schema.ts b/apps/api/src/modules/ledger/schema.ts
index 0183857..da26f4e 100644
--- a/apps/api/src/modules/ledger/schema.ts
+++ b/apps/api/src/modules/ledger/schema.ts
@@ -6,10 +6,9 @@
  * outbound FKs to still-flat tables (goals, insurance_policies, sips,
  * statement_reconciliations) and 23 inbound FK columns from still-flat tables
  * into these 11, so physically relocating the table definitions here would
- * create a genuine bidirectional ES-module cycle with `db/schema.ts` (unlike
- * task 0.3's single-table `projection_settings` case). Table definitions stay
- * in `db/schema.ts`, unmoved, until task 1.9's cross-module FK-graph/SCC work
- * decides a final, acyclic home for each one.
+ * create a genuine bidirectional ES-module cycle with `db/schema.ts`. Table
+ * definitions stay in `db/schema.ts`, unmoved, until task 1.9's cross-module
+ * FK-graph/SCC work decides a final, acyclic home for each one.
  *
  * Services/routes inside `modules/ledger/` import table objects from this
  * local file (never reaching into `../../db/schema.ts` directly for
@@ -18,11 +17,9 @@
  * to change this one file, not every service/route that already imports from
  * `./schema.ts`.
  *
- * `db/schema.ts` does NOT `export *` back from this file — unlike
- * `projection_settings` (a single-owner table where the barrel genuinely
- * needs the module's only copy), the ledger tables' only home is still
- * `db/schema.ts` itself, so the reverse direction would just recreate a
- * pointless cycle.
+ * `db/schema.ts` does NOT `export *` back from this file — the ledger tables'
+ * only home is still `db/schema.ts` itself, so the reverse direction would
+ * just recreate a pointless cycle (same reasoning as all five modules).
  */
 export {
   accounts,
diff --git a/apps/api/src/modules/planning/schema.ts b/apps/api/src/modules/planning/schema.ts
index dc6d429..1d6018c 100644
--- a/apps/api/src/modules/planning/schema.ts
+++ b/apps/api/src/modules/planning/schema.ts
@@ -1,13 +1,33 @@
-import { integer, pgTable, timestamp, uuid } from "drizzle-orm/pg-core";
-import { users } from "../../db/core-schema.ts";
-
-/** Per-user assumptions used only for forward-looking goal projections. */
-export const projectionSettings = pgTable("projection_settings", {
-  userId: uuid("user_id")
-    .primaryKey()
-    .references(() => users.id, { onDelete: "cascade" }),
-  /** Broad-equity annual return assumption (1200 = 12%). */
-  equityReturnBps: integer("equity_return_bps").notNull().default(1200),
-  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
-  updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
-});
+/**
+ * Thin, named re-export of the planning domain's 6 tables + 2 owned enums.
+ *
+ * This is deliberately NOT where the `pgTable()`/`pgEnum()` calls live —
+ * planning is now uniform with the other four modules (ledger, credit,
+ * investments, protection). `goals.id` has inbound FKs from `accounts`,
+ * `holdings` and `sips`; `budget_lines`/`budget_alerts` have outbound FKs
+ * to `categories`. Physically relocating the table definitions here would
+ * create a genuine cross-file ES-module cycle with `db/schema.ts`. Table
+ * definitions stay in `db/schema.ts`, unmoved, until task 1.9's cross-module
+ * FK-graph/SCC work decides a final, acyclic home for each one.
+ *
+ * Services/routes inside `modules/planning/` import table objects from this
+ * local file (never reaching into `../../db/schema.ts` directly for
+ * planning-owned tables) — this is the module-boundary discipline that matters:
+ * it costs nothing today and means a future physical decomposition only has
+ * to change this one file, not every service/route that already imports from
+ * `./schema.ts`.
+ *
+ * `db/schema.ts` does NOT `export *` back from this file — the planning tables'
+ * only home is still `db/schema.ts` itself, so the reverse direction would
+ * just recreate a pointless cycle (same reasoning as the other four modules).
+ */
+export {
+  budgets,
+  budgetLines,
+  budgetAlerts,
+  goals,
+  subscriptionDismissals,
+  projectionSettings,
+  budgetPeriod,
+  goalType,
+} from "../../db/schema.ts";
```

`git diff -- apps/api/src/db/schema.smoke.test.ts` produced **no output** — confirmed empty/unmodified.

### New untracked file: `apps/api/src/modules/planning/schema.smoke.test.ts` (70 lines, full content)

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import type pg from "pg";
import { getTableConfig } from "drizzle-orm/pg-core";
import { createDb } from "../../db/index.ts";
import * as barrel from "../../db/schema.ts";
import * as planningSchema from "./schema.ts";

// Object-identity proof: modules/planning/schema.ts is a thin re-export, not an
// accidental duplicate definition. Every one of the 6 planning tables (and their
// 2 owned enums) imported via the module path must be the exact same object
// as the one imported via the db/schema.ts barrel — not just structurally
// equal. Mirrors modules/credit/schema.smoke.test.ts.

const TABLE_NAMES: Record<string, string> = {
  budgets: "budgets",
  budgetLines: "budget_lines",
  budgetAlerts: "budget_alerts",
  goals: "goals",
  subscriptionDismissals: "subscription_dismissals",
  projectionSettings: "projection_settings",
} as const;

const ENUM_NAMES = ["budgetPeriod", "goalType"] as const;

test("modules/planning/schema.ts re-exports the same 6 table objects as db/schema.ts with correct SQL names", () => {
  for (const [name, sqlName] of Object.entries(TABLE_NAMES)) {
    const tableObj = (planningSchema as Record<string, unknown>)[name];
    const barrelObj = (barrel as Record<string, unknown>)[name];
    assert.strictEqual(
      tableObj,
      barrelObj,
      `${name}: modules/planning/schema.ts must re-export the identical object as db/schema.ts`,
    );
    const config = getTableConfig(tableObj as never);
    assert.equal(config.name, sqlName, `${name}: SQL table name must be "${sqlName}"`);
  }
});

test("modules/planning/schema.ts re-exports the same 2 owned enum objects as db/schema.ts", () => {
  for (const name of ENUM_NAMES) {
    assert.strictEqual(
      (planningSchema as Record<string, unknown>)[name],
      (barrel as Record<string, unknown>)[name],
      `${name}: modules/planning/schema.ts must re-export the identical enum object as db/schema.ts`,
    );
  }
});

test("a real createDb() instance (non-connecting stub pool) exposes db.query for all 6 planning tables at runtime", () => {
  // A stub pg.Pool that would throw if drizzle ever tried to use it — proving
  // no query is issued and no connection is opened during construction.
  const stubPool = {
    query: () => {
      throw new Error("stub pool must never be queried by this test");
    },
    connect: () => {
      throw new Error("stub pool must never be connected to by this test");
    },
  } as unknown as pg.Pool;

  const db = createDb(stubPool);

  for (const name of Object.keys(TABLE_NAMES)) {
    assert.ok(
      (db.query as Record<string, unknown>)[name],
      `db.query.${name} must exist on the constructed Drizzle instance`,
    );
  }
});
```

## 3. `npm run typecheck`

```
> compass@0.1.0 typecheck
> npm run typecheck --workspaces --if-present

> @compass/api@0.1.0 typecheck
> tsc --noEmit

> @compass/docs@0.1.0 typecheck
> tsc --noEmit

> @compass/extractor@0.1.0 typecheck
> tsc --noEmit

> @compass/ingestor@0.1.0 typecheck
> tsc --noEmit

> @compass/web@0.1.0 typecheck
> tsc --noEmit

> @compass/ai@0.1.0 typecheck
> tsc --noEmit

> @compass/shared@0.1.0 typecheck
> tsc --noEmit
```
Exit code: 0

## 4. `npm run lint`

```
> compass@0.1.0 lint
> eslint .
```
Exit code: 0

## 5. `npm run test -w apps/api`

Tail of output:
```
✔ reportToCsv emits the necessity rows with distinct labels and values (1.570527ms)
ℹ tests 845
ℹ suites 1
ℹ pass 845
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 7912.023724
```
Exit code: 0
**845 pass, 0 fail** — matches expected 845 (baseline 842 + 3).

## 6. From `apps/api`: `node --test src/modules/planning/schema.smoke.test.ts`

```
✔ modules/planning/schema.ts re-exports the same 6 table objects as db/schema.ts with correct SQL names (1.833719ms)
✔ modules/planning/schema.ts re-exports the same 2 owned enum objects as db/schema.ts (0.202ms)
✔ a real createDb() instance (non-connecting stub pool) exposes db.query for all 6 planning tables at runtime (2.793543ms)
ℹ tests 3
ℹ suites 0
ℹ pass 3
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 937.722136
```
Exit code: 0. **Exactly 3 test cases, all pass — confirmed.**

## 7. From `apps/api`: `node --env-file-if-exists=../../.env --test src/services/backup.test.ts`

```
✔ the full backup covers every table in the schema (2.072047ms)
✔ sips precedes holding_events in ALL_TABLES (holding_events.sip_id FKs sips) (0.233311ms)
✔ the per-user export reconstructs every table (no coverage gaps) (0.258956ms)
✔ no table is scoped both directly and through a parent (0.211806ms)
✔ every storage-key column in the schema is covered by FILE_COLUMNS (0.65589ms)
✔ collectFileRefs pulls every non-empty storage key from a dump (0.478542ms)
✔ the per-user restore covers exactly the exported tables, in parent-first order (0.40314ms)
✔ restore defers cyclic and self-referencing foreign keys (0.469987ms)
✔ restoreDump's second pass issues an update for every column in DEFERRED_RESTORE_COLUMNS (1.435671ms)
✔ misc-05 AC14: restoreDump's first pass carries user_tasks.source/source_key through untouched when present, and omits them (falling back to the column DEFAULT) when the dump predates the migration (0.715967ms)
✔ AC11: a task linked to an owned transaction, and an unlinked task, round-trip through per-user backup/restore (319.690971ms)
✔ misc-05 AC14: the per-user archive round-trips a card-due task's source/sourceKey through restoreUserBackup, alongside an ordinary task (162.148743ms)
✔ misc-05 AC14: a per-user archive predating source/sourceKey (missing both keys entirely) restores via restoreUserBackup by falling back to the column DEFAULTs (34.425034ms)
ℹ tests 13
ℹ suites 0
ℹ pass 13
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 1447.160174
```
Exit code: 0

## 8. Snapshot hashes

```
$ sha256sum apps/api/src/route-surface.snapshot.txt apps/api/src/route-table.snapshot.txt
a368d4ebfcb6bd9638ae24a3334e5b7fe61798e3cb82bdec12a06e93becc4122  /home/udai/PennyPilot/apps/api/src/route-surface.snapshot.txt
7800feb971c2e570040a299addf207960a0866f2a7feb377c4a2cf84bf4255c8  /home/udai/PennyPilot/apps/api/src/route-table.snapshot.txt
```
Both **match** the expected hashes given in the brief exactly (`a368d4eb…4122` and `7800feb9…55c8`). **PASSED** — nothing touched routes.

## 9. CRITICAL GATE — `db/schema.smoke.test.ts` unmodified proof

```
$ git diff -- apps/api/src/db/schema.smoke.test.ts
(no output)
```
File is **byte-identical to HEAD** — confirmed via empty diff.

```
$ cd apps/api && node --test src/db/schema.smoke.test.ts
✔ schema barrel exposes users and projectionSettings exactly once, with correct table names/columns (2.01126ms)
✔ a real createDb() instance (non-connecting stub pool) exposes db.query.users and db.query.projectionSettings at runtime (3.242293ms)
ℹ tests 2
ℹ suites 0
ℹ pass 2
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 908.472422
```
Exit code: 0. **Gate PASSED**: the load-bearing identity proof holds — `projectionSettings` imported from the planning module path is the same object the barrel exports, verified without any modification to the test that asserts it.

## 10. AC2 checks

```
$ grep -c "pgTable(" apps/api/src/modules/planning/schema.ts
1
$ grep -c "pgEnum(" apps/api/src/modules/planning/schema.ts
1
```
**Literal grep result is NOT 0 for either.** Investigated why:
```
$ grep -n "pgTable(\|pgEnum(" apps/api/src/modules/planning/schema.ts
4: * This is deliberately NOT where the `pgTable()`/`pgEnum()` calls live —
```
The single match in each case is a **false positive from doc-comment prose** — the new doc comment's sentence
"This is deliberately NOT where the `pgTable()`/`pgEnum()` calls live" contains the literal substrings
`pgTable(` and `pgEnum(` inside backticked code-font text. Reading the file confirms there is genuinely **zero
actual `pgTable()`/`pgEnum()` call** in `modules/planning/schema.ts` — the file is exactly one `export { ... }
from "../../db/schema.ts";` statement plus the doc comment. So the AC2 **intent** (no table/enum definitions in
the module file) is satisfied, but the brief's literal `grep -c` command as specified does **not** return 0.
Flagging this as a deviation from the literal instruction, not a defect in the code.

```
$ git show HEAD:apps/api/src/db/schema.ts | grep -c "pgTable("
49
$ grep -c "pgTable(" apps/api/src/db/schema.ts   # working tree
51
```
**Literal delta is +2, not +1.** Same root cause: the new doc comment inserted into `db/schema.ts` (line 31,
"Every `pgTable()`/`pgEnum()` definition lives here — the single Drizzle Kit entry point.") also contains the
substring `pgTable(`, contributing one false-positive match. To separate the doc-comment noise from the real
code, I diffed the line-numbered `pgTable(` grep matches between HEAD and working tree:
```
$ diff <(git show HEAD:apps/api/src/db/schema.ts | grep -n "pgTable(") <(grep -n "pgTable(" apps/api/src/db/schema.ts)
```
This showed the HEAD list (49 `export const X = pgTable(...)` declaration lines) is otherwise reproduced
line-for-line (with a uniform +10 line-number shift after the insertion point) in the working-tree list, **plus
exactly two new lines**: the doc-comment line 31 (false positive) and the real new declaration
`export const projectionSettings = pgTable("projection_settings", {` at line 749. No existing declaration was
added, removed, or reordered relative to HEAD. **Semantically the real-code delta is +1** (matching AC2's
intent); the **literal grep-count delta the brief specifies is +2** because of the comment-text false positive.
This is worth reporting plainly rather than silently reconciling: the coordinator should decide whether the
doc-comment wording is acceptable given this side effect on the literal AC2 check as written.

## 11. AC3 check — `db/schema.ts` local-schema imports

```
$ grep -n 'from "' apps/api/src/db/schema.ts
1:import { sql } from "drizzle-orm";
19:} from "drizzle-orm/pg-core";
20:import { users } from "./core-schema.ts";
21:export { users } from "./core-schema.ts";
```
Every `from "..."` specifier in the file: `"drizzle-orm"`, `"drizzle-orm/pg-core"`, `"./core-schema.ts"` (x2:
one import, one re-export). **No import or re-export from any `modules/` path.** AC3 **PASSED**.

## 12. T18 — `projectionSettings` block verbatim proof

Extracted the doc-comment + `pgTable(...)` block + closing `});` (10 lines) from both sources:

Old (`git show HEAD:apps/api/src/modules/planning/schema.ts`, lines 4–13):
```
/** Per-user assumptions used only for forward-looking goal projections. */
export const projectionSettings = pgTable("projection_settings", {
  userId: uuid("user_id")
    .primaryKey()
    .references(() => users.id, { onDelete: "cascade" }),
  /** Broad-equity annual return assumption (1200 = 12%). */
  equityReturnBps: integer("equity_return_bps").notNull().default(1200),
  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
});
```

New (`apps/api/src/db/schema.ts`, lines 748–757):
```
/** Per-user assumptions used only for forward-looking goal projections. */
export const projectionSettings = pgTable("projection_settings", {
  userId: uuid("user_id")
    .primaryKey()
    .references(() => users.id, { onDelete: "cascade" }),
  /** Broad-equity annual return assumption (1200 = 12%). */
  equityReturnBps: integer("equity_return_bps").notNull().default(1200),
  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
});
```

```
$ diff old_block.txt new_block.txt
(no output, exit 0)
```
**Empty diff — character-identical**, including the doc comment and all four columns in the specified order
(`user_id`, `equity_return_bps`, `created_at`, `updated_at`). **T18 PASSED.**

## 13. AC10 — `db:generate` zero-diff proof

Manifest before (`find apps/api/drizzle -type f | sort | xargs sha256sum`): **135 files**.

```
$ npm run db:generate
...
No config path provided, using default 'drizzle.config.ts'
Reading config file '/home/udai/PennyPilot/apps/api/drizzle.config.ts'
51 tables
account_nps_details 9 columns 0 indexes 2 fks
...
projection_settings 4 columns 0 indexes 1 fks
...
users 7 columns 0 indexes 0 fks

No schema changes, nothing to migrate 😴
```
Exit code: 0.

Manifest after: **135 files** (same count).
```
$ diff manifest_before.txt manifest_after.txt
(no output, exit 0)
```
**Manifests are identical, zero diff. AC10 PASSED.** `db:generate` ran successfully against the configured
DB (`DATABASE_URL` was resolvable via `.env`); it was not skipped.

## 14. AC12/T17 — resolver-based import-specifier check

Wrote a throwaway Node script in the scratchpad
(`/tmp/claude-1001/-home-udai-PennyPilot/ad09ead0-26c7-444d-9b89-3b727c4e538e/scratchpad/resolve-specifiers.mjs`,
not committed to the repo) that walks every `*.ts` under `apps/api/src`, extracts specifiers from all four
forms (`import ... from "..."` incl. `import type`, `export ... from "..."`, bare `import "...";`, dynamic
`import("...")`), keeps only relative (`.`-prefixed) specifiers, and resolves each against the importing
file's directory trying, in order: exact path, `+ ".ts"`, `+ "/index.ts"`, accepting only a regular file
(`fs.statSync(...).isFile()`).

```
Files scanned: 224
Relative specifiers scanned: 681
Unresolved count: 0
```
**0 unresolvable specifiers.** Magnitudes (224 files / 681 specifiers) are close to but not identical to the
brief's expected "~223 files / ~686 specifiers" (review-3's reproduced figures) — within the brief's own
"roughly" tolerance, and materially the same order of magnitude, so I judge the extractor is not missing a
form category. **AC12/T17 PASSED** with this caveat noted for the coordinator's awareness (small scan-count
discrepancy of -1 file / -5 specifiers vs. the review-3 baseline, most plausibly explained by the new smoke
test file's specifier count vs. whatever else changed the tree since review-3's run; I did not chase this
further as it was not asked for and 0 unresolved is unambiguous either way).

## 15. AC13 — `CLAUDE.md` diff

Already shown in full in section 2. Confirmed: **exactly one line changed** (`CLAUDE.md:49`, the
"Transitional module scaffold" bullet). The new text no longer claims `db/schema.ts` "re-exports each
module's `schema.ts`" and no longer claims every `modules/<domain>/schema.ts` imports from
`db/core-schema.ts`; it instead states the barrel holds every `pgTable()`/`pgEnum()` definition, module
`schema.ts` files are thin named re-exports, and cites task 1.9 as the eventual owner of physical
per-module ownership. **AC13 PASSED.**

## 16. Scope check

```
$ git status --porcelain -- apps/api/src/routes/ apps/api/src/services/ apps/api/src/app.ts
(no output)
$ git diff --stat -- apps/api/src/routes/ apps/api/src/services/ apps/api/src/app.ts
(no output)
```
**No file under `apps/api/src/routes/`, `apps/api/src/services/`, and `app.ts` was modified.** Full
`git status --porcelain` (repeated from section 1) shows only the 4 modified files + 1 new test file +
2 unrelated untracked task-record paths — **nothing moved or deleted**. Scope check **PASSED**.

Minor observation (not a scope violation, reported for completeness): DELEGATION.md's own instructions say
the sonnet-worker must "Write full findings to `tasks/014-migrate-planning/implementation-1.md`" — this file
was **not present** in `tasks/014-migrate-planning/` at verification time (only `backend-1.md`,
`investigation-1/2.md`, `review-1/2/3.md`, `TASK.md`, `DELEGATION.md` exist). Not something I fixed or was
asked to check, just flagging that the delegation's own evidence-file requirement appears unmet.

---

## Summary of gate results

| Check | Result |
|---|---|
| typecheck | PASS (exit 0) |
| lint | PASS (exit 0) |
| `test -w apps/api` | PASS — 845/845, exit 0 |
| `modules/planning/schema.smoke.test.ts` | PASS — exactly 3 cases |
| `services/backup.test.ts` | PASS — 13/13 |
| route-surface / route-table snapshot hashes | UNCHANGED, match expected exactly |
| **Step 9 gate** (`db/schema.smoke.test.ts` unmodified + green) | **PASSED** — diff empty, 2/2 tests pass |
| **AC2** | Semantic intent PASSED (0 real `pgTable`/`pgEnum` calls in planning/schema.ts; +1 real declaration in db/schema.ts, no reorder). **Literal grep-count as specified in the brief is NOT 0/+1** — it is 1/1 and +2 — due to the new doc comments' prose containing the literal substrings `pgTable(`/`pgEnum(`. Reported as a deviation, not silently reconciled. |
| **AC3** | PASSED — db/schema.ts's only local schema specifier is `./core-schema.ts` |
| **AC10** | PASSED — `db:generate` reports "No schema changes, nothing to migrate"; 135-file manifest identical before/after |
| **AC12** | PASSED — 0 unresolvable relative specifiers (224 files / 681 specifiers scanned) |
| **AC13** | PASSED — exactly one CLAUDE.md line changed, false propositions removed |
| T18 verbatim proof | PASSED — extract diff empty |
| Scope (routes/services/app.ts untouched) | PASSED |
